function Footer() {
    return (
        <footer className="footer">
            <p>
                Backend: <code>http://localhost:4000/api/query</code> —
                Make sure CORS is enabled on the server.
            </p>
        </footer>
    )
}

export default Footer
